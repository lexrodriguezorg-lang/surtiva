{
  "name": "surtiva",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Surtiva: plataforma comercial multiempresa con acceso aprobado",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "dev": "node --env-file-if-exists=.env.local scripts/build.mjs && node --env-file-if-exists=.env.local scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
